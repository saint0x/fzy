# OPTIMIZE.md

Note: this document is a live issue list, not a permanent archive. When an issue is fully implemented, validated, and verified with the relevant Fozzy-first checks, remove that issue from this file entirely so the list gets progressively smaller and eventually disappears when all optimization work is done. -- included in this round of refactors and improvements is refactoring large consolidated/potentially future unmaintainable files so you can feel free to safely refactor as well --and any compiler bugs that these refactors/upgrades may expose should be taken seriously in full and we shoudl fix those fro mthe architectural core correctly, not paper over them.

5. HIR lowering repeatedly scans the module, rebuilds environment maps, and clones function bodies into typed structures, making it one of the highest-latency compiler phases before native codegen. `hir::lower` builds `struct_defs`, `enum_defs`, `trait_defs`, `trait_impls`, and `type_aliases` through separate whole-module scans in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:337), then clones function bodies into `TypedFunction` values in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:581). This should be split into staged declaration indexing and body inference passes with shared immutable metadata so large-module compile time scales better.

6. HIR hot paths repeatedly allocate empty maps and use string-keyed lookups where immutable shared defaults or interned ids would be materially cheaper. Item checking paths construct fresh `HashMap::new()` placeholders for globals and mutability state in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:437) and [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:498), and the file broadly relies on `HashMap<String, ...>` for high-volume compiler metadata. This should be tightened by reusing singleton empty maps and gradually moving hot symbol/type lookups to interned ids rather than repeated owned strings.

12. `driver::command.rs` and `driver::pipeline.rs` are both monolithic enough that they likely hurt not just runtime behavior but also incremental Rust compile times and maintainability of future perf work. The files are currently about 19.6k and 15k lines respectively, and they mix orchestration, policy, I/O, formatting, diagnostics, and command-family specifics into extremely large compilation units. They should be split by command family and phase responsibility so that profiling, caching, and optimization changes can be targeted without dragging unrelated code through rebuilds.

17. The runtime executor currently favors deterministic and readable data structures over raw host-mode throughput, which is fine semantically but should not be the last word for production latency-sensitive paths. Task storage and related metadata in [crates/runtime/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/runtime/src/lib.rs:1) rely on `BTreeMap`-centric organization and record full event traces eagerly, while timeout checks call `Instant::now()` on execution in [crates/runtime/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/runtime/src/lib.rs:526). Host-mode fast implementations such as slab-backed task tables, lighter trace modes, and compact event storage should be added without weakening deterministic behavior where it is required.

18. The stdlib HTTP surface is too large and mixed-responsibility to optimize effectively as-is, and it likely hides avoidable string, map, and parsing allocations across transport and protocol concerns. [crates/stdlib/src/http.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/http.rs:1) combines socket lifecycle, request parsing, header handling, payload logic, and deterministic backend behavior into one 2.5k-line module. It should be split into parser, transport, header, and payload layers so borrowed parsing and focused benchmarking can be applied where they matter most.

19. The stdlib observability layer serializes the same log entry separately for multiple sinks and clones memory-backed log state on reads, which turns logging into more CPU and allocation work than necessary under load. `Logger::emit` in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:117) calls `serde_json::to_string(entry)` independently for file, UDP, and stdout sinks, and `memory_entries` clones the full in-memory vector in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:108). Logging should pre-render shared sink payloads once per entry and use bounded or snapshot-oriented memory sinks instead of cloning full accumulated state.

20. The stdlib text surface allocates owned `String` results even for operations that could remain borrowed, which lines up with the already-observed text-kernel gaps in the benchmark docs. `split`, `trim`, and `slice_at_utf8_boundaries` in [crates/stdlib/src/text.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/text.rs:20) all allocate owned strings by design. Borrowed or view-based variants should be introduced internally or in lowering paths so loop-local string operations stop forcing allocation when the result does not escape.

21. `apps/live_server` uses a mutex-serialized receiver for worker handoff and a busy-sleep accept loop, which limits throughput and adds scheduler noise even though the service otherwise already improved substantially. The server wraps `Receiver<TcpStream>` in `Arc<Mutex<_>>` in [apps/live_server/src/main.rs](/Users/deepsaint/Desktop/fozzylang/apps/live_server/src/main.rs:290), sleeps on `WouldBlock` in [apps/live_server/src/main.rs](/Users/deepsaint/Desktop/fozzylang/apps/live_server/src/main.rs:315), and allocates a fresh request buffer per connection in [apps/live_server/src/main.rs](/Users/deepsaint/Desktop/fozzylang/apps/live_server/src/main.rs:439). This should move to a readiness-based or blocking-accept design with per-worker buffers and a queue handoff that does not serialize all workers behind one mutex.

22. `apps/live_server` store durability still clones full in-memory state before serialization and uses request-local string formatting on the hot path, which adds unnecessary latency as traffic or state size grows. `flush_store_now` clones the entire `StoreData` snapshot in [apps/live_server/src/main.rs](/Users/deepsaint/Desktop/fozzylang/apps/live_server/src/main.rs:145), while request id and trace naming allocate strings in [apps/live_server/src/main.rs](/Users/deepsaint/Desktop/fozzylang/apps/live_server/src/main.rs:448). Batched durability with cheaper request id generation and eventually a WAL-plus-snapshot model would keep durability semantics while reducing hot-path overhead.

23. The codebase lacks enough built-in phase-level performance telemetry, which means future regressions will continue to show up first as "the compiler feels slower" rather than as localized phase regressions. There is benchmark evidence in [PERF.md](/Users/deepsaint/Desktop/fozzylang/PERF.md) and command coverage via Fozzy, but the compiler and CLI paths still do not expose standard per-phase timings, cache hit rates, bytes read, or artifact I/O timings by default. A real perf telemetry surface should be added so every optimization pass can be measured and every completed issue in this file can be removed with evidence rather than intuition.

24. Several HIR analysis passes clone large branch-local binding maps at every `if`/`else` and loop split, which creates avoidable superlinear CPU and allocator pressure on control-flow-heavy modules. GPU contract analysis clones binding environments in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:2250), and live-borrow analysis repeats the same pattern in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:3812). These analyses should switch to delta-based branch state, persistent maps, or reversible mutation stacks so control-flow validation does not keep copying whole `BTreeMap` environments.

25. FIR dataflow analysis is currently string- and set-heavy in a way that will become a major latency sink as CFG size grows. `compute_liveness` in [crates/fir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/fir/src/lib.rs:480) builds `BTreeSet<String>` live-in/live-out sets, clones them during fixed-point iteration, and repeatedly unions successor state by string key. This should move toward symbol ids plus bitset or index-based liveness so verifier and backend preparation stop paying ordered-string-set costs on every iteration.

27. Deterministic stdlib collection wrappers currently hard-code ordered tree structures everywhere, which preserves stable iteration but also bakes `O(log n)` lookup and allocation-heavy behavior into hot runtime surfaces that do not always need full sort-order semantics. `DeterministicMap` and `DeterministicSet` in [crates/stdlib/src/collections.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/collections.rs:63) are `BTreeMap`/`BTreeSet` wrappers, and this pattern echoes through runtime-facing modules such as HTTP and observability. The codebase should separate "stable iteration" from "tree-backed storage" and introduce cheaper deterministic containers or hybrid representations for hot paths that only need reproducible order at boundaries.

28. Cold parse misses rebuild a giant concatenated source blob and merge every module AST into one owned aggregate, which adds avoidable string growth, allocator churn, and tree movement before HIR even begins. `parse_program_uncached_with_root_source` in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:7117) parallel-loads module ASTs, appends every module into `combined_source`, removes each loaded module from the map, and merges all imported ASTs into the root. This should move toward a graph-backed parsed representation with lazily materialized combined text only for the specific consumers that actually need it, so cold `check`, `verify`, and deterministic planning paths stop paying full merge cost by default.

29. Native backend emission lacks content-addressed artifact reuse, so identical FIR plus identical backend/profile/link settings still rebuild objects, runtime shims, archives, and linked outputs on repeated runs. LLVM emission in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14681) rewrites `.ll` and re-invokes the external toolchain each time, while Cranelift emission in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14748) and library emission in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14273) rebuild objects and runtime shims unconditionally. The build pipeline should cache backend artifacts by FIR hash plus profile, backend, runtime-shim inputs, and linker-affecting settings so warm `build` and `run` latency drops substantially without weakening correctness.

30. Deterministic non-scenario `run` and `test` flows currently duplicate orchestration work by first synthesizing bridge artifacts from language analysis and then rerouting through scenario execution for host-backed modes. Host-backed deterministic routing in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:408) and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:805) generates a trace path, runs `run_non_scenario_test_plan_with_root_guidance`, extracts a generated scenario, and then immediately invokes `fzscenario::run_scenario` or `run_tests` on that derived scenario. These flows should share one execution model or at least one reusable intermediate package so host-backed deterministic commands do not pay both the language-side planning cost and the scenario-side execution setup cost back to back.

31. Trace serialization currently pays a guaranteed double-encode tax in order to compute checksums, which becomes a real wall-clock and allocation cost on large traces. `TraceFile::write_json` in [crates/fzscenario/src/runtime/tracefile.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/tracefile.rs:211) serializes once without checksum, hashes that byte vector, then serializes the full payload again with checksum included before writing the temp file. This should be reworked into a streaming or staged checksum format, or otherwise cached at the field level, so recording and CI verification traces remain safe but cheaper on trace-heavy workloads.

32. Replay-contract derivation rescans all decisions and events after a run instead of collecting the same metadata incrementally while the trace is already being produced. `build_replay_contract` in [crates/fzscenario/src/runtime/tracefile.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/tracefile.rs:379) walks every decision and every event to reconstruct execution order, async schedule, capability sets, RPC frames, runtime event counts, and causal links. That replay metadata should be accumulated during run construction so trace finalization becomes mostly assembly rather than a second full trace pass.

33. Deep scenario doctor mode reruns full scenarios and rehashes full run payloads for every audit iteration, which is fine for rigor but unnecessarily expensive as currently implemented. `doctor` in [crates/fzscenario/src/runtime/doctor.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/doctor.rs:49) loops `runs` times through `run_scenario_inner`, then `scenario_run_signature` in [crates/fzscenario/src/runtime/doctor.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/doctor.rs:154) reserializes status, memory, findings, decisions, and events into JSON before hashing. The audit should preserve deep confidence while using cheaper canonical signatures, optional artifact reuse, and clearer "audit mode" ergonomics so deterministic health checks stay thorough without being needlessly slow.

34. `fz check` and `fz verify` still collapse to the same heavyweight verification path in practice, so the fastest developer feedback command is paying nearly the same semantic cost as the stricter command. Both command arms in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:970) and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:976) route through `verify_file_with_root_guidance`, and that path in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:501) still performs backend capability diagnostics, native lowerability diagnostics, full FIR verification, compile-time contract diagnostics, and Kernel IR diagnostics. The CLI should expose a genuinely cheap validation tier so edit-check loops stop paying strict-verify-style work unless that depth is explicitly requested.

35. The LSP implementation rebuilds workspace semantics from scratch for each hover, definition, completion, signature-help, document-symbol, and workspace-symbol request, which will create editor latency long before full compiler throughput becomes the bottleneck. `build_workspace_semantics` in [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:206) rereads and reparses workspace documents, and request handlers such as [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1656), [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1681), [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1706), [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1760), [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1798), and [crates/driver/src/lsp.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/lsp.rs:1826) all rebuild that state on demand. This should become an incremental, version-aware semantic cache keyed by document and workspace state so common editor actions stay effectively warm.

36. The host HTTP backend still uses an `O(n)` readiness scan and synthetic poll-event requeueing model that is structurally expensive for persistent connections and likely explains part of the remaining persistent-HTTP benchmark gap. `scan_poll_interests` in [crates/stdlib/src/http.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/http.rs:347) walks every registered interest and pushes synthetic events, and `poll_next` in [crates/stdlib/src/http.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/http.rs:761) repopulates the queue from that scan whenever the queue is empty. This backend should move toward actual readiness tracking or a cheaper event-state model so persistent HTTP paths stop paying full interest-set scans between operations.

39. LLVM IR lowering is still built around stringly IR assembly plus aggressive stack-slot materialization, which means compiler time is being spent on formatting, hashmap churn, and alloca-heavy emission before LLVM even gets a chance to optimize the result. `LlvmFuncCtx` in [crates/driver/src/pipeline/llvm_support.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/llvm_support.rs:54) stores mutable IR text in `code: String` and `alloca_prologue: String`, while `declare_alloca` in [crates/driver/src/pipeline/llvm_support.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/llvm_support.rs:136) is hit from many lowering paths across the file. This backend should move toward a more SSA-friendly intermediate builder with fewer slot spills and less raw string concatenation so compiler latency drops and backend optimization has less cleanup work to do.

40. Cranelift emission still rebuilds and clones more per-function semantic state than necessary, which makes the supposedly fast dev backend pay extra setup cost on every compile. Both native artifact emission paths build fresh function-signature and descriptor maps in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14325) and [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14795), and each function passes `function.local_types.clone()` into `clif_emit_function_cfg` in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14496) and [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:14971). The Cranelift path should share immutable per-module metadata and stop cloning large local type maps per function so warm dev builds better reflect the backend’s intended low-latency role.

41. Reporting, profile, and artifact-inspection commands repeatedly reload summaries, traces, and derived profile payloads instead of sharing one hydrated run bundle, which makes non-execution CLI workflows slower than they need to be. `report_doc_with_profile` in [crates/fzscenario/src/cmd/reporting_cmd.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/reporting_cmd.rs:102) layers profile diagnosis on top of separately loaded run data, `load_summary` falls back to reading traces in [crates/fzscenario/src/cmd/reporting_cmd.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/reporting_cmd.rs:223), and `profile_support` reloads profile JSON artifacts and can regenerate them from traces in [crates/fzscenario/src/cmd/profile_support.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/profile_support.rs:8) and [crates/fzscenario/src/cmd/profile_support.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/profile_support.rs:694). These command families should share a single cached run/profile bundle so `report`, `profile`, and `artifacts` flows stop multiplying disk reads and JSON decode work.

42. The Fozzy CI gate currently performs several heavyweight checks serially with very little reuse, which makes correctness validation more expensive than it needs to be on every trace. `ci_evaluate` in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:47) verifies the trace, rereads it in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:78), replays it in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:79), exports a zip bundle in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:130), reopens the archive in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:137), streams every file through a sink in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:146), and may then run profile diffing in [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:184). CI should reuse one loaded trace context and make export, replay, and perf checks more selectively composable so routine gate latency does not balloon as more checks are added.

43. Repository-topology and corpus-management commands still rely on broad `WalkDir` scans of whole directories, which will become a user-visible latency problem on large workspaces and large corpora. `scan_repo` and scenario discovery in [crates/fzscenario/src/cmd/map_cmd.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/map_cmd.rs:768) and [crates/fzscenario/src/cmd/map_cmd.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/map_cmd.rs:906) walk full trees for hotspot and suite mapping, while corpus listing and import/export paths do the same in [crates/fzscenario/src/cmd/corpus.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/corpus.rs:39) and [crates/fzscenario/src/cmd/corpus.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/corpus.rs:128). These commands should gain manifest-aware root narrowing, literal-path fast paths, and cached scan facts so repo-scale tooling remains responsive as the project grows.

44. The parser still lexes each source file into a fully owned token buffer before parse-time consumption, which makes `check`, `verify`, and especially LSP-style feedback pay an eager allocation cost proportional to total file size even when only a subset of structure is needed. `parse` in [crates/parser/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/parser/src/lib.rs:113) constructs `Lexer`, materializes `tokens = lexer.lex()`, and only then begins syntactic analysis through `Parser::new(tokens, module_name)` in [crates/parser/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/parser/src/lib.rs:122), while `TokenKind` stores owned `String` payloads for identifiers and string literals in [crates/parser/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/parser/src/lib.rs:12). The frontend should move toward borrowed token slices, lighter token payloads, or an incremental parse representation so edit-check and editor latency stop paying full eager token ownership costs on every request.

45. Warm compiler cache lookups still hash the entire concatenated module source before they can hit, so cache-hit latency remains `O(total_source_bytes)` even when parsed and lowered state is otherwise reusable. `lower_fir_cached` computes `module_hash = sha256_hex(parsed.combined_source.as_bytes())` in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:700), and that `combined_source` itself is rebuilt as a giant merged string during cold parses in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:7131). This should switch to a cheaper incremental fingerprint derived from per-module stamps or cached content hashes so warm `check`, `verify`, and `build` runs do not spend noticeable time rehahsing the full logical program just to discover a cache hit.

46. Parallel scenario testing currently uses ad hoc scoped thread spawning with a central channel receive loop, which adds thread-management overhead and leaves scheduling quality below what a reused worker pool or work-stealing executor could provide for large suites. `run_parallel_tests` in [crates/fzscenario/src/runtime/test_runner.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/test_runner.rs:161) repeatedly `scope.spawn`s one thread per in-flight scenario, clones run options into each worker, and funnels completion through a single `rx.recv()` loop. The runner should use a persistent bounded worker pool or task executor so multi-scenario throughput scales more smoothly without paying repeated thread creation and centralized handoff overhead on every suite run.

47. Trace verification on the read path still clones and reserializes the entire trace just to recompute the checksum, which means `trace verify`, `replay`, and other trace-loading tools pay an avoidable full-payload copy even after the trace is already decoded. `verify_checksum` in [crates/fzscenario/src/runtime/tracefile.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/tracefile.rs:920) does `let mut canonical = trace.clone(); canonical.checksum = None; let bytes = serde_json::to_vec(&canonical)?;` before hashing. This should be replaced with a checksum strategy that can validate from the original bytes or from a lighter canonical view so read-heavy trace workflows stop paying a second full in-memory materialization.

48. Execution artifact assembly is still duplicated across `run`, `explore`, and `fuzz`, which increases the risk that future optimization work lands in one path while the others keep the slower behavior. `run_scenario` in [crates/fzscenario/src/runtime/run_flow.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/run_flow.rs:23), `explore_steps_scenario` and adjacent explore flows in [crates/fzscenario/src/modes/explore.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/modes/explore.rs:297) and [crates/fzscenario/src/modes/explore.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/modes/explore.rs:220), and fuzz crash/report assembly in [crates/fzscenario/src/modes/fuzz.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/modes/fuzz.rs:267) each rebuild broadly similar summary, trace, events, timeline, memory-artifact, and reporter-output flows with their own cloning and serialization patterns. These paths should converge on one shared run-artifact builder so performance fixes to summary assembly, profile generation, and heavy-artifact gating apply uniformly instead of being reimplemented piecemeal.

49. Shrink currently reruns whole scenarios from scratch for each candidate reduction, cloning step vectors and embedded scenario state repeatedly without any memoization of previously tested candidates or reuse of baseline-derived facts. `shrink_trace_inner` in [crates/fzscenario/src/runtime/run_flow.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/run_flow.rs:284) clones `scenario.steps` into `candidate`, rebuilds `ScenarioV1Steps` for each trial in [crates/fzscenario/src/runtime/run_flow.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/run_flow.rs:351), and calls `run_embedded_scenario_inner` again for every chunk candidate in [crates/fzscenario/src/runtime/run_flow.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/run_flow.rs:357). This should gain candidate fingerprint caching, cheaper structural views over step ranges, and incremental shrink bookkeeping so large failing traces do not turn shrink into a repeated full-execution grinder.

50. The stdlib observability surface stores every metric sample and span in unbounded vectors and computes percentiles by cloning and sorting full histograms on demand, which will become a real latency and memory sink under sustained load. `Metrics` in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:161) appends every counter, gauge, and histogram sample into `Vec<MetricPoint>`, `histogram` materializes a new vector in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:221), `percentile` sorts that copied vector in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:233), and `Tracer` retains all spans in memory in [crates/stdlib/src/observability.rs](/Users/deepsaint/Desktop/fozzylang/crates/stdlib/src/observability.rs:266). This should move toward bounded aggregation, streaming percentile sketches, and retention-aware span storage so observability remains useful without turning on a silent throughput tax.

51. The validation command surface still has stale flag drift between the shipped CLI and docs/scripts, which causes avoidable failed invocations, retry churn, and duplicated compatibility handling in automation. The current `fz` entrypoint rejects `test --strict` and requires `--strict-verify` in [crates/fz/src/entry.rs](/Users/deepsaint/Desktop/fozzylang/crates/fz/src/entry.rs:210) and [crates/fz/src/entry.rs](/Users/deepsaint/Desktop/fozzylang/crates/fz/src/entry.rs:254), while shipped guidance still advertises `fozzy test --det --strict ...` in [PERF.md](/Users/deepsaint/Desktop/fozzylang/PERF.md:40) and `fz test --det --strict ...` in [USAGE.md](/Users/deepsaint/Desktop/fozzylang/USAGE.md:100). The command surface should converge on one stable spelling, with docs/tests/scripts regenerated from the same source, so validation loops stop paying failure-and-retry overhead and operator automation stops carrying needless branch logic for legacy flags.

52. Parsed-program cache hits still serialize callers behind a global mutex and restat every module file before returning, so warm-path latency keeps a built-in lock-and-filesystem tax even before the cloned `ParsedProgram` is handed back. `cached_parsed_program` in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:7194) takes the global `PARSED_PROGRAM_CACHE` lock, walks every stored stamp, and calls `module_stamp` on each path before declaring a hit. This should move to read-optimized cache concurrency plus cheaper invalidation state so warm `check`, `verify`, `build`, and editor-oriented requests do not bottleneck on one mutex and a full per-module metadata pass.

53. Wildcard import expansion reparses imported module source yet again after the module graph has already been discovered and qualified, which means some module sets are paying a built-in third parse just to enumerate exported names. `expand_wildcard_imports` in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:7365) resolves the wildcard target and then calls `parser::parse(&target_module.source, target_module_name)` again in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:7406) before iterating the target items. Wildcard expansion should reuse the already parsed AST or a cached declaration index so import-heavy module graphs stop doing extra frontend work just to expand names.

54. Scenario checkpointing currently clones the entire execution world for rollback, which makes nested-step validation, failure-expected branches, and any future exhaustive reduction work pay worst-case copy costs in proportion to all tracked runtime state. `checkpoint` in [crates/fzscenario/src/runtime/engine.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/engine.rs:2917) clones RNG state, virtual clock state, KV and FS stores, host filesystem snapshots, proc and HTTP rules, network queues and inboxes, partition state, and memory tracking before `restore` writes it all back in [crates/fzscenario/src/runtime/engine.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/engine.rs:2940). This should move toward delta-based rollback, persistent data structures, or reversible mutation logs so exhaustive and adversarial scenario tooling can scale without clone-the-world latency spikes.

55. The CLI JSON output path still parses and reformats the same payload multiple times after command execution, which means machine-readable invocations pay extra CPU work that is unrelated to the command they actually asked for. The entrypoint normalizes command output in [crates/fz/src/entry.rs](/Users/deepsaint/Desktop/fozzylang/crates/fz/src/entry.rs:49), then reparses the original JSON again in `infer_exit_code` in [crates/fz/src/entry.rs](/Users/deepsaint/Desktop/fozzylang/crates/fz/src/entry.rs:57), while JSON normalization itself does a `serde_json::from_str` plus `to_string_pretty` roundtrip in [crates/driver/src/cli_output.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/cli_output.rs:54). Structured command results should carry exit code and presentation separately so `--json` callers stop paying repeated decode and re-encode overhead after the real work is already done.

56. Native runtime string handling is still built around one global mutex plus linear duplicate scans, which makes string-heavy workloads pay an avoidable synchronization and `O(n)` interning tax on the hot path. `fz_lookup_string`, `fz_intern_owned`, and `fz_intern_slice` in [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:304) all serialize on `fz_string_lock`, and `fz_intern_owned` linearly scans both the static literal table and all dynamic strings before inserting in [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:330). This should move toward a read-optimized string table with cheaper lookup or dedup structure and a non-interned fast path for loop-local temporaries so text, JSON, HTTP, and logging paths stop paying a global lock-and-scan penalty.

57. Native numeric vector access still takes the coarse global collections mutex on every length and element read, which is a direct throughput limiter for bytes-like kernels and any loop that lowers through the handle-based numeric vector path. `fz_native_vec_len`, `fz_native_vec_get_i32`, `fz_native_vec_get_u32`, and `fz_native_vec_get_f32` in [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:479) all lock `fz_collections_lock` around each access, even when the operation is a simple indexed read from a numeric buffer. The native data plane should gain direct-memory or at least per-buffer read-friendly access paths so hot numeric loops stop paying a mutex roundtrip per element.

58. The native runtime shim still centralizes unrelated arrays, vectors, maps, and aggregates behind a single global collections lock, which means independent subsystems contend with each other even when they touch disjoint runtime state. Collection state in [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:85) shares `fz_collections_lock`, and that same lock is used across numeric vectors, aggregates, and map-like operations such as the helpers around [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:497) and [crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline/native_runtime_support/runtime_shim/core.rs:552). This should be split into narrower synchronization domains or lock-free/read-mostly structures so unrelated runtime features do not serialize each other under mixed workloads.

59. Native run supervision currently polls child state and optional health/smoke endpoints with a fixed 50 ms sleep loop, which adds built-in tail latency and coarse-grained responsiveness to bounded-run and service-smoke workflows. `run_native_binary_with_bounds` in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:1434) repeatedly calls `try_wait`, optional HTTP probes, and then `thread::sleep(Duration::from_millis(50))` before checking again. This should move toward event-driven waiting or at least adaptive polling so `fz run --max-seconds`, `--exit-on-healthcheck`, and `--smoke-http` do not pay a fixed scheduler-delay tax on every successful or timeout-bounded invocation.

60. The `fzscenario` crate currently re-exports nearly every command, mode, model, platform, and runtime module from one umbrella public surface, which increases incremental Rust compile coupling and makes command-local changes more likely to invalidate unrelated code. The crate root in [crates/fzscenario/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/lib.rs:1) wires command, runtime, model, and platform modules together through `#[path = ...]` declarations and broad `pub use` re-exports, while command-heavy modules such as [crates/fzscenario/src/cmd/profile_support.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/profile_support.rs:1), [crates/fzscenario/src/cmd/ci.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/cmd/ci.rs:1), and [crates/fzscenario/src/runtime/run_flow.rs](/Users/deepsaint/Desktop/fozzylang/crates/fzscenario/src/runtime/run_flow.rs:1) all hang off that same crate boundary. The scenario toolchain should separate stable shared runtime/model APIs from command-specific frontends so profiling, CI, reporting, and corpus work do not drag the whole crate through rebuilds or widen the rebuild impact of unrelated changes.

61. Common CLI verbs still attach extra documentation and interop generation work that is not essential to the fast-path answer the caller asked for, which adds filesystem, parse, and render overhead to routine local loops. `fz build` may immediately follow compilation with header generation and interop finalization in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:341) and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:359), while `build`, `test`, `check`, and `verify` also opportunistically call `maybe_generate_unsafe_docs` from paths such as [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:350), [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:484), [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:973), and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:979). These secondary artifact flows should become explicitly requested, profile-gated, or content-addressed cached so common compile-and-validate commands stop paying extra work unrelated to success/failure, diagnostics, or the built artifact itself.

63. `fz check` and `fz verify` still collapse onto the same heavyweight implementation, so the CLI exposes two validation verbs without any actual latency tiering between them. Both command branches in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:970) and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:976) call the same `verify_file_with_root_guidance`, and that path in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:501) still resolves a native backend, runs backend-capability and lowerability diagnostics, lowers through HIR and FIR, invokes the strict verifier, runs compile-time contract diagnostics, and lowers kernel IR before returning in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:553). The command surface should grow a genuinely cheaper structural-validation tier for `check`, leaving the full strict semantic and policy-heavy pipeline to `verify`, so routine editor and local-loop usage stop paying release-grade validation costs by default.

64. The core performance benchmark feedback loop is currently brittle because at least one shipped scratch benchmark fixture no longer builds cleanly, which blocks fast remeasurement and makes optimization work harder to validate continuously. The robust Rust-vs-Fzy bench runner in [scripts/bench_corelibs_rust_vs_fzy.py](/Users/deepsaint/Desktop/fozzylang/scripts/bench_corelibs_rust_vs_fzy.py:239) currently shells out to `fz build` for fixtures such as [examples/benchmarks/resultx_scratch_bench.fzy](/Users/deepsaint/Desktop/fozzylang/examples/benchmarks/resultx_scratch_bench.fzy:60), and a fresh local run now fails with parser diagnostics at lines 60, 67, 74, 81, and 87 instead of producing new measurements. The performance harness and benchmark fixtures should be treated as first-class production assets with their own compatibility gate, so perf work does not lose rapid measurement coverage due to syntax drift or stale benchmark sources.

68. The compiler's internal phase boundaries are still blurrier than they need to be, which creates duplicated semantic carriers and repeated lowering work across `HIR`, `FIR`, verifier, and native backend preparation. `TypedModule` and `TypedFunction` in [crates/hir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/hir/src/lib.rs:7) remain the semantic source for many downstream checks, `FirModule` still carries full `typed_functions` alongside lowered CFG data in [crates/fir/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/fir/src/lib.rs:82), the verifier still consumes those high-level functions directly in [crates/verifier/src/lib.rs](/Users/deepsaint/Desktop/fozzylang/crates/verifier/src/lib.rs:159), and native codegen builds another canonical control-flow/data plan in [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:10310) and [crates/driver/src/pipeline.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/pipeline.rs:11213). The optimization direction here should be to sharpen the existing contracts rather than add a premature general MLIR tier: keep `HIR` as the semantic truth, make `FIR` or its immediate successor the single downstream analysis contract, promote the native canonical plan into a clearer first-class lowering stage, and reserve any MLIR-style integration for a future dedicated GPU `Kernel IR` path instead of adding another broad middle layer to the whole compiler.

69. Native `fz run` currently accepts per-backend host flags without actually routing native/project-root runs onto the host-backed runtime path, which is both a correctness bug and a source of wasted debugging/perf cycles. The CLI parser for `run` only derives `host_backends` from `--host-backends` in [crates/fz/src/entry.rs](/Users/deepsaint/Desktop/fozzylang/crates/fz/src/entry.rs:228), while the native routing logic in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:377) and [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:611) keys off that boolean alone. As a result, `fz run <project> --proc-backend host --fs-backend host --http-backend host --json` silently reports `"hostBackends": false` and runs as plain `"native"` mode, even though `fz run <project> --host-backends --json` correctly routes to `"native-host-runtime"`. This should be fixed at the CLI/driver boundary by either mapping the explicit per-backend host triple onto native host-backed routing or rejecting those flags with a clear diagnostic for native/project-root runs, and it should get dedicated regression coverage next to the existing host-backend run tests in [crates/driver/src/command.rs](/Users/deepsaint/Desktop/fozzylang/crates/driver/src/command.rs:15513).
