// core.gpu
// Capability import for GPU programs plus optional high-level helpers.
// Canonical GPU operations live in the runtime `gpu.*` namespace directly.
// This module intentionally does not redefine intrinsic names.
host fn has_device() -> i32 {
    if gpu.device_count() > 0 {
        return 1
    }
    return 0
}
