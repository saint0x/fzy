use core.error;

// Canonical public stdlib surface.
mod text;
mod bytes;
mod duration;
mod io;
mod process;
mod term;
mod http;
mod log;
mod mem;
mod thread;
mod security;
mod result;
mod simd;
mod network;

// Internal support modules. These are implementation detail shelves, not
// canonical product-facing namespaces.
mod capability;
mod retry;
mod concurrency;
// FFI contracts live in language/tooling surfaces, not in a public `core.*`
// import path.
mod abi;
mod cinterop;

pub fn version() -> str {
    return "0.2.0"
}
