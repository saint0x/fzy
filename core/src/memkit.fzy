use core.mem;

fn freeze() -> i32 {
    mem.freeze()
    return 0
}

fn unfreeze() -> i32 {
    mem.unfreeze()
    return 0
}

fn phase_fence() -> i32 {
    discard freeze()
    discard unfreeze()
    return 0
}

fn sample() -> i32 {
    return phase_fence()
}
