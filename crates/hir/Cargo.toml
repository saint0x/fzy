[package]
name = "hir"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
ast = { path = "../ast" }
core = { path = "../core" }

[dev-dependencies]
parser = { path = "../parser" }
