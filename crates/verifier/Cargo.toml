[package]
name = "verifier"
version.workspace = true
edition.workspace = true

[dependencies]
fir = { path = "../fir" }
diagnostics = { path = "../diagnostics" }
core = { path = "../core" }
ast = { path = "../ast" }
