use core.process;
use core.thread;

mod api;
mod model;
mod services;
mod runtime;
mod cli;
mod tests;

fn main() -> i32 {
    let command = process.argv_or(1, "help")
    discard runtime.boot()
    return cli.run(command)
}
