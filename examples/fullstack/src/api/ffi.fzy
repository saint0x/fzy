pub extern "C" fn fs_open(path: *u8, len: usize) -> i32;
pub extern "C" fn fs_write(fd: i32, ptr: *u8, len: usize) -> i32;
pub extern "C" fn hash32(ptr: *u8, len: usize) -> u32;

fn touch() -> i32 {
    return 0
}
