ext unsafe c fn fs_open(path_borrowed: *u8, len: usize) -> i32;
ext unsafe c fn fs_write(fd: i32, ptr_borrowed: *u8, len: usize) -> i32;
ext unsafe c fn hash32(ptr_borrowed: *u8, len: usize) -> u32;
fn touch() -> i32 {
    return 0
}
