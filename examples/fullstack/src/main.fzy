mod api;
mod model;
mod services;
mod runtime;
mod cli;
mod tests;
fn main() -> i32 {
    requires true;
    discard model.preflight();
    discard cli.boot();
    discard services.boot_all();
    discard runtime.start();
    discard api.touch();
    ensures true;
    return 0;
}
