#[repr(C)]
struct UserRow {}

#[repr(C, packed)]
enum SyncState {}

fn schema_version() -> i32 {
    let major: i32 = 1
    let minor: i32 = 3
    return major + minor
}

fn classify_state(code: i32) -> i32 {
    match code { 0 => 1, 1 => 2, _ => 3 }
}
