mod auth;
mod store;
mod replication;
mod metrics;
mod http;
use auth::init as auth_init;
fn boot_all() -> i32 {
    discard auth_init();
    discard store.init();
    discard replication.init();
    discard metrics.init();
    discard http.start();
    return 0;
}
