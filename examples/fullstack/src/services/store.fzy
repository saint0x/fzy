use cap.fs;
use cap.mem;

fn put_user() -> i32 {
    let ptr: *u8 = alloc(64)
    defer free(ptr)
    let file = fs.open()
    defer close(file)
    return 0
}

fn get_user() -> i32 {
    let ptr: *u8 = alloc(64)
    defer free(ptr)
    return 1
}
