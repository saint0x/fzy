use cap.time;
use cap.fs;
use cap.net;

fn helper() -> i32 {
    let a: i32 = 40
    let b: i32 = 2
    return a + b
}

fn main() -> i32 {
    let base: i32 = 20
    let delta: i32 = 22
    defer fs.flush()
    net.connect()
    time.now()
    return base + delta
}
