[package]
name = "live_server"
version = "0.1.0"

[[target.bin]]
name = "live_server"
path = "src/main.fzy"

[ffi]
panic_boundary = "error"

[unsafe]
contracts = "compiler"
enforce_dev = false
enforce_verify = true
enforce_release = true
