#[ffi_panic(error)] pub extern "C" fn server_hash32(ptr_borrowed: *u8, len: usize) -> u32;
#[ffi_panic(error)] pub extern "C" fn server_write(fd: i32, ptr_borrowed: *u8, len: usize) -> i32;
fn touch() -> i32 {
    return 0
}
