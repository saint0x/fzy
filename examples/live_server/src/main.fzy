use cap.time;
use cap.rng;
use cap.fs;
use cap.net;
use cap.proc;
use cap.mem;
use cap.thread;

mod api;
mod model;
mod services;
mod runtime;
mod cli;
mod tests;

fn main() -> i32 {
    model.preflight()
    cli.boot()
    services.boot_all()
    runtime.start()
    api.touch()
    return 0
}
