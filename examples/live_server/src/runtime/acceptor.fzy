use core.thread;
use core.http;
fn run() -> i32 {
    let listener: HttpHandle = http.bind() http.listen(listener) let conn: HttpHandle = http.accept() http.read(conn) close(conn) close(listener) checkpoint() return 0
}
