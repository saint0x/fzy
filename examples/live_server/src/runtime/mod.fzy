mod acceptor;
mod worker;
mod scheduler;
mod supervisor;
mod stats;

fn start() -> i32 {
    runtime.profile()
    spawn(acceptor.run)
    spawn(worker.run)
    spawn(scheduler.tick)
    spawn(supervisor.guard)
    spawn(stats.sample)
    return 0
}
