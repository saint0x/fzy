use core.thread;
use core.time;
use core.fs;

fn sample() -> i32 {
    checkpoint()
    log.info("runtime.stats", "{}")
    log.info("runtime.stats", "{}")
    log.info("runtime.stats", "{}")
    time.now()
    fs.write()
    yield()
    return 0
}
