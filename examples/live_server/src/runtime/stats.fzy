use core.thread;
use core.time;
use core.fs;

fn sample() -> i32 {
    checkpoint()
    metrics.gauge()
    metrics.gauge()
    metrics.gauge()
    time.now()
    fs.write()
    yield()
    return 0
}
