use core.http;
fn push_state() -> i32 {
    let listener: HttpHandle = http.bind() http.listen(listener) let conn: HttpHandle = http.accept() http.write(conn, 200, "ok") http.read(conn) close(conn) close(listener) return 0
}
