mod scheduler;
mod worker;

fn start_worker() -> i32 {
    spawn(worker.run);
    return 0;
}

fn start_scheduler() -> i32 {
    spawn(scheduler.tick);
    return 0;
}

fn start() -> i32 {
    discard start_worker();
    discard start_scheduler();
    return 0;
}
