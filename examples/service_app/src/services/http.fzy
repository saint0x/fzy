use core.http;
use core.time;

fn start() -> i32 {
    let listener_owned: HttpHandle = http.bind();
    http.listen(listener_owned);
    close(listener_owned);
    discard time.now();
    return 0;
}
