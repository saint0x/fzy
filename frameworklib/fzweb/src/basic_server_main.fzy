use core.thread;
use core.storage;
mod webcore;
mod app;
mod cookies;
mod middleware;
mod multipart;
mod persistence;
mod sessions;
mod streaming;
mod support;
mod request;
mod response;
mod routing;
fn main() -> i32 {
    return support.run_live_server()
}
