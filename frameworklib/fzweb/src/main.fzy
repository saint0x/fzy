use core.term;

mod api;
mod model;
mod services;
mod runtime;
mod cli;
mod tests;

fn main() -> i32 {
    discard term.is_interactive()
    discard runtime.boot()
    discard cli.boot()
    return runtime.surface_score()
}
