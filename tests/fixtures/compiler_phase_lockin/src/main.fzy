use core.term;
mod services;
mod model;

fn main() -> i32 {
    let total = services.boot() + model.types.flavor_score()
    discard term.write("compiler-phase-lockin\n")
    if total == 12 {
        return 0
    }
    return 91
}
