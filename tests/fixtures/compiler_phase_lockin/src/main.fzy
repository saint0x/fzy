use core.term;
mod services;
mod model;
use services::*;
use model.types::*;

fn main() -> i32 {
    let total = boot() + flavor_score()
    discard term.write("compiler-phase-lockin\n")
    if total == 12 {
        return 0
    }
    return 91
}
