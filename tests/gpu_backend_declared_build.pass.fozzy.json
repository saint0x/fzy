{
  "version": 1,
  "name": "gpu-backend-declared-build-pass",
  "steps": [
    {
      "type": "proc_when",
      "cmd": "python3",
      "args": [
        "-c",
        "import json, pathlib, subprocess, tempfile, textwrap; repo = pathlib.Path.cwd(); root = pathlib.Path(tempfile.mkdtemp(prefix='fozzylang-gpu-build-declared-')); (root / 'src').mkdir(parents=True, exist_ok=True); (root / 'fozzy.toml').write_text(textwrap.dedent('''\\\n            [package]\n            name=\"gpu_backend_declared\"\n            version=\"0.1.0\"\n\n            [[target.bin]]\n            name=\"gpu_backend_declared\"\n            path=\"src/main.fzy\"\n        ''')); (root / 'src/main.fzy').write_text(textwrap.dedent('''\\\n            use core.gpu;\n            kernel fn copy(input: GpuSlice<f32>, output: GpuSlice<f32>, n: i32) -> void {\n                let i = gpu.global_id_x();\n                if i < n {\n                    output[i] = input[i];\n                }\n            }\n            host fn main() -> i32 {\n                let dev = gpu.default_device();\n                let input = gpu.alloc_f32(dev, 4);\n                defer gpu.free(input);\n                let output = gpu.alloc_f32(dev, 4);\n                defer gpu.free(output);\n                let event = gpu.launch3(copy, 1, 64, gpu.slice(input, 0, 4), gpu.slice(output, 0, 4), 4);\n                gpu.wait(event);\n                return 0;\n            }\n        ''')); verify = subprocess.run(['cargo', 'run', '-q', '-p', 'fz', '--', 'verify', str(root), '--json'], cwd=str(repo), capture_output=True, text=True); assert verify.returncode == 0, (verify.returncode, verify.stdout, verify.stderr); verify_payload = json.loads(verify.stdout.strip()); verify_messages = [item['message'] for item in verify_payload['items']]; assert not any('gpu backend `metal` is declared in the architecture but not yet executable' in message for message in verify_messages), verify_messages; package_payload = json.loads((root / '.fz' / 'gpu-kernel-package.json').read_text()); assert package_payload['schemaVersion'] == 'fozzylang.gpu_kernel_package.v1', package_payload; assert package_payload['status'] == 'ok', package_payload; assert 'copy' in package_payload['kernels'], package_payload; assert 'kernel.entry copy' in package_payload['renderedKernelIr'], package_payload; build = subprocess.run(['cargo', 'run', '-q', '-p', 'fz', '--', 'build', str(root), '--json'], cwd=str(repo), capture_output=True, text=True); assert build.returncode == 1, (build.returncode, build.stdout, build.stderr); build_payload = json.loads(build.stdout.strip()); build_messages = [item['message'] for item in build_payload['items']]; assert any('gpu backend `metal` does not yet execute these GPU operations' in message for message in build_messages), build_messages; assert not any('native backend cannot execute unresolved call `gpu.' in message for message in build_messages), build_messages; print('gpu-backend-declared-build-ok')"
      ],
      "exit_code": 0,
      "stdout": "gpu-backend-declared-build-ok\n",
      "stderr": "",
      "times": 1
    },
    {
      "type": "proc_spawn",
      "cmd": "python3",
      "args": [
        "-c",
        "import json, pathlib, subprocess, tempfile, textwrap; repo = pathlib.Path.cwd(); root = pathlib.Path(tempfile.mkdtemp(prefix='fozzylang-gpu-build-declared-')); (root / 'src').mkdir(parents=True, exist_ok=True); (root / 'fozzy.toml').write_text(textwrap.dedent('''\\\n            [package]\n            name=\"gpu_backend_declared\"\n            version=\"0.1.0\"\n\n            [[target.bin]]\n            name=\"gpu_backend_declared\"\n            path=\"src/main.fzy\"\n        ''')); (root / 'src/main.fzy').write_text(textwrap.dedent('''\\\n            use core.gpu;\n            kernel fn copy(input: GpuSlice<f32>, output: GpuSlice<f32>, n: i32) -> void {\n                let i = gpu.global_id_x();\n                if i < n {\n                    output[i] = input[i];\n                }\n            }\n            host fn main() -> i32 {\n                let dev = gpu.default_device();\n                let input = gpu.alloc_f32(dev, 4);\n                defer gpu.free(input);\n                let output = gpu.alloc_f32(dev, 4);\n                defer gpu.free(output);\n                let event = gpu.launch3(copy, 1, 64, gpu.slice(input, 0, 4), gpu.slice(output, 0, 4), 4);\n                gpu.wait(event);\n                return 0;\n            }\n        ''')); verify = subprocess.run(['cargo', 'run', '-q', '-p', 'fz', '--', 'verify', str(root), '--json'], cwd=str(repo), capture_output=True, text=True); assert verify.returncode == 0, (verify.returncode, verify.stdout, verify.stderr); verify_payload = json.loads(verify.stdout.strip()); verify_messages = [item['message'] for item in verify_payload['items']]; assert not any('gpu backend `metal` is declared in the architecture but not yet executable' in message for message in verify_messages), verify_messages; package_payload = json.loads((root / '.fz' / 'gpu-kernel-package.json').read_text()); assert package_payload['schemaVersion'] == 'fozzylang.gpu_kernel_package.v1', package_payload; assert package_payload['status'] == 'ok', package_payload; assert 'copy' in package_payload['kernels'], package_payload; assert 'kernel.entry copy' in package_payload['renderedKernelIr'], package_payload; build = subprocess.run(['cargo', 'run', '-q', '-p', 'fz', '--', 'build', str(root), '--json'], cwd=str(repo), capture_output=True, text=True); assert build.returncode == 1, (build.returncode, build.stdout, build.stderr); build_payload = json.loads(build.stdout.strip()); build_messages = [item['message'] for item in build_payload['items']]; assert any('gpu backend `metal` does not yet execute these GPU operations' in message for message in build_messages), build_messages; assert not any('native backend cannot execute unresolved call `gpu.' in message for message in build_messages), build_messages; print('gpu-backend-declared-build-ok')"
      ],
      "expect_exit": 0,
      "expect_stdout": "gpu-backend-declared-build-ok\n"
    }
  ]
}
